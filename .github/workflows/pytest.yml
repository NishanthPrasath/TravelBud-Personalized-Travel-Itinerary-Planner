name: Pytest

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r Requirements.txt

    - name: Run pytest
      run: pytest test.py
      env:
        SECRET_KEY: ${{ FINAL_PROJ_ENV.SECRET_KEY }}
        ALGORITHM: ${{ FINAL_PROJ_ENV.ALGORITHM }}
        GOOGLE_MAPS_API_KEY: ${{ FINAL_PROJ_ENV.GOOGLE_MAPS_API_KEY }}
